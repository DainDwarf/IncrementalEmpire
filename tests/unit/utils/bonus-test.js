import bonus from 'incremental-empire/utils/bonus';
import { module, test } from 'qunit';

module('Unit | Utility | bonus', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = bonus();
    assert.ok(result);
  });
});
