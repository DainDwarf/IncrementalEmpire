<div class="container-fluid">
  <div class="row">
    <nav class="nav nav-pills flex-column border-right">
      {{#link-to "universe.empire.ressources.food" class="nav-link"}}
        Food <span class="badge badge-pill badge-light">{{model.food}}</span>
      {{/link-to}}
      {{#link-to "universe.empire.ressources.wood" class="nav-link"}}
        Wood <span class="badge badge-pill badge-light">{{model.wood}}</span>
      {{/link-to}}
      {{#link-to "universe.empire.ressources.stone" class="nav-link"}}
        Stone <span class="badge badge-pill badge-light">{{model.stone}}</span>
      {{/link-to}}
      {{#link-to "universe.empire.ressources.metal" class="nav-link"}}
        Metal <span class="badge badge-pill badge-light">{{model.metal}}</span>
      {{/link-to}}
      {{#link-to "universe.empire.ressources.energy" class="nav-link"}}
        Energy <span class="badge badge-pill badge-light">{{model.energy}}</span>
      {{/link-to}}
    </nav>
    <div class="ml-2 mt-2">
      {{outlet}}
    </div>
  </div>
</div>