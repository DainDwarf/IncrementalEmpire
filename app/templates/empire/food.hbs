<div>
  {{model.name}} has {{model.foodStorage}} {{if (gt model.foodStorage 1) "granaries" "granary"}} and can store a maximum of {{model.maxFood}} food.
  <AssignPoint @class="ml-2" @disabled={{model.dead}}
               @value={{model.pendingFoodStorage}} @onChange={{action "changePendingFoodStorage"}}
               @step={{empireCtl.assignValue}} @max={{maxPendingFoodStorage}}>
    Construct more granaries: {{model.pendingFoodStorage}} (100 material)
  </AssignPoint>
  <button type="button"
          class="btn btn-outline-success"
          hidden={{not isGenFoodStorageAvailable}}
          disabled={{isGenFoodStorageDisabled}}
          onclick={{action "genFoodStorage"}}>
    Holy Building <DisplayValue @tagName="small" @type="magic" @value="50" />
  </button>
</div>
<hr>
<div>
  {{model.name}} has {{model.food}} food.
  {{#if model.foodProduction}}
    You generate {{model.foodProduction}} food per turn.
  {{/if}}
  {{#if empireCtl.isLowFood}}
    <div class="text-danger">
      You will lose some population next turn.
    </div>
  {{/if}}
  <AssignPoint @class="ml-2" @isVisible={{workerHunterAvailable}} @disabled={{model.dead}}
               @value={{model.workerHunter}} @onChange={{action "changeHunter"}}
               @step={{empireCtl.assignValue}} @max={{maxHunter}}>
    Hunter{{if (gt model.workerHunter 1) "s"}}: {{model.workerHunter}}
  </AssignPoint>
  <button type="button"
          class="btn btn-outline-success"
          hidden={{not isGenFoodAvailable}}
          disabled={{isGenFoodDisabled}}
          onclick={{action "genFood"}}>
    Horn of Plenty <DisplayValue @tagName="small" @type="magic" @value="1" />
  </button>
</div>
<div>
  {{model.name}} has {{model.hunterStorage}} hunting grounds. You can have a maximum of {{model.maxWorkerHunter}} hunters.
  <AssignPoint @class="ml-2" @disabled={{model.dead}}
               @value={{model.pendingHunterStorage}} @onChange={{action "changePendingHunterStorage"}}
               @step={{empireCtl.assignValue}} @max={{maxPendingHunterStorage}}>
    Construct more hunting grounds: {{model.pendingHunterStorage}} (100 material)
  </AssignPoint>
  <button type="button"
          class="btn btn-outline-success"
          hidden={{not isGenHunterStorageAvailable}}
          disabled={{isGenHunterStorageDisabled}}
          onclick={{action "genHunterStorage"}}>
    Holy Building <DisplayValue @tagName="small" @type="magic" @value="50" />
  </button>
</div>
{{outlet}}