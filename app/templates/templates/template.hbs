<div class="container-fluid bg-light">
  <Input value={{model.name}} @focus-out={{action "updateTemplateName"}}/> the {{model.type}} empire
  <span class="float-right">You have {{remainingTemplatePoints}}/{{templatePoints}} template points</span>
</div>
<AssignPoint @class="ml-2" @onLess={{action "lessPop"}} @onMore={{action "morePop"}}
             @canBeLess={{gt model.popTP 0}} @canBeMore={{gt remainingTemplatePoints 0}}>
  Population: {{rebirthPop}} ({{model.popTP}} TP)
</AssignPoint>
<AssignPoint @class="ml-2" @onLess={{action "lessFood"}} @onMore={{action "moreFood"}}
             @canBeLess={{gt model.foodTP 0}} @canBeMore={{gt remainingTemplatePoints 0}}>
  Food: {{rebirthFood}} ({{model.foodTP}} TP)
</AssignPoint>
<div class="container">
  <button type="button" class="btn btn-outline-danger my-2" onclick={{action "rebirth"}}>
    <div>Restart with this {{model.type}} empire</div>
    <small>Gain <DisplayValue @tagName="span" @type={{game.rebirthPointsType}} @value={{game.rebirthPoints}} /> from current {{game.empire.type}} empire</small>
  </button>
</div>
<div class="container small">
  {{#if (eq model.type "religious")}}
    Religious empires produces more <DisplayValue @tagName="span" @type="mana" @value="" /> if you have more population and spend more turns. You need at least 2 population and 20 turns to start producing <DisplayValue @tagName="span" @type="mana" @value="" />
  {{else if (eq model.type "economical")}}
    Economical empires produces more <DisplayValue @tagName="span" @type="money" @value="" /> if you created more ressources, but decreases if you take too much turns. You need at least 20 turns to start producing <DisplayValue @tagName="span" @type="money" @value="" />.
  {{/if}}
</div>
{{outlet}}